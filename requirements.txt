Django>=5.2,<5.3
whitenoise>=6.6,<7
openpyxl>=3.1,<4
requests>=2.31,<3
numpy>=1.24
Pillow>=10.0

# rasterio (+ native GDAL libraries) is only needed to render raster .tif
# overlays from data/Raster/. It is large and routinely cannot be installed or
# loaded on serverless platforms like Vercel, where it was crashing the whole
# function. The app now detects its absence and disables ONLY the raster-overlay
# endpoints, so everything else works. To work with rasters locally, install it
# explicitly (ideally with a GDAL-capable environment):
#     pip install "rasterio>=1.3,<2"
